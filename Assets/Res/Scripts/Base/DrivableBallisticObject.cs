using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 弹道驱动物体
/// </summary>
public class DrivableBallisticObject : DrivableObject
{

    /// <summary>
    /// 目标物体。起始点，终点，飞行速度
    /// </summary>
    public override void Init(params object[] param)
    {
        base.Init(param);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
